<br />
<br />
<br />
<br />
<br />
<br />
<div id="errors">
	{{#if error}}
		<p class="error">{{error}}</p>
	{{/if}}
</div>
<p>username: username must not already be taken by another user, and must be between 5 and 10 characters.</p>
<p>email: must be a valid email, containing no whitespace </p>
<p>password: must not contain any whitespace, must contain at least one
<p>password: must not contain any whitespace, must contain at least one
	uppercase letter, at least one digit, at least one special character among
	@$!%*?&amp;, and must be at least 8 characters long.
</p>
<form action="signup" method="POST" name="signup-form" id="signup-form">
	<label for="username">Username
		<input
			type="text"
			name="username"
			id="username"
			value="{{prevSub.username}}"
		/>
	</label>
	<br />
	<label for="email">Email
		<input type="text" name="email" id="email" value="{{prevSub.email}}" />
	</label>
	<br />
	<label for="password">Password
		<input
			type="password"
			name="password"
			id="password"
			value="{{prevSub.password}}"
		/>
	</label>
	<br />
	<label for="confirmPassword">Confirm Password
		<input
			type="password"
			name="confirmPassword"
			id="confirmPassword"
			value="{{prevSub.confirmPassword}}"
		/>
	</label>
	<br />
	<button type="submit" name="submitButton" id="submitButton">Submit</button>
</form>
<a href="/auth/login">Already have an account? Click here to log-in.</a>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="/public/js/signup.js"></script>