<link rel="stylesheet" href=".././public/css/home.css">
<article>
    <h1 id="title">Home</h1>
    <ul id="reviewList">
    {{#each review}}
        <li class="review">
            <section>
                <a href="/view/review/{{review._id}}">{{review.title}}</a>
                <a href="/view/dish/{{review.dishId}}">{{review.dishname}}</a>
                <a href="/view/user/{{review.userId}}">{{review.username}}</a>
            </section>
        </li>
    {{/each}}
    </ul>
</article>